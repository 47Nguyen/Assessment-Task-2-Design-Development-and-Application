# COSC2753 Assignment 2 — pinned so every member runs the same versions.
# Install with:  pip install -r requirements.txt

numpy==1.26.4
pandas==2.3.3
scikit-learn==1.3.1
scikit-image==0.22.0        # HOG + colour-histogram features for the classical baselines (Tasks 1 & 3)
Pillow==12.0.0
matplotlib==3.10.7
seaborn==0.13.0
joblib==1.3.2
tqdm==4.66.1

# Deep learning
tensorflow==2.15.0
protobuf<5.0.0              # TF 2.15 breaks on protobuf >=5 ("MessageFactory has no attribute GetPrototype")

# Notebook tooling
jupyter==1.0.0
nbdime==4.0.1               # sane git diffs/merges for .ipynb — run `nbdime config-git` once after installing
